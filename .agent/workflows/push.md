---
description: Pushes local changes to the GitHub repository to sync with Lovable.
---

To sync your local changes with Lovable, run the following command:

// turbo
1. Run the push command:
```bash
git push origin main
```

If you are prompted for a username and password, use your GitHub username and a Personal Access Token (PAT) as the password.
